title: EndpointDetails
type: object
properties:
  dropoff:
    type: object
    properties:
      type:
        type: string
        enum: [merchant, customer]
        description: Type of endpoint.
      type_id:
        type: string
        description: Identifier of the type of the endpoint.
      address:
        $ref: '#/components/schemas/Address'
