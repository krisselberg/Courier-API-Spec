description: Email address associated with the courier account for which the password reset is requested
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        Email:
          type: string
          format: email
          description: Email address of the courier
      required:
        - Email
