OAuth2:
  type: oauth2
  flows:
    authorizationCode:
      authorizationUrl: https://example.com/oauth/authorize
      tokenUrl: https://example.com/oauth/token
      scopes:
        driver: Access to driver's actions
# NodeAuth:
#     type: apiKey
#     in: header
#     name: X-Stamp
